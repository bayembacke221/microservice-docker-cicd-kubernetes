server:
  port: 8082
spring:
  application:
    name: service-examen
  datasource:
    password: password
    url: jdbc:postgresql://postgres:5432/examen
    username: bmbacke
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true