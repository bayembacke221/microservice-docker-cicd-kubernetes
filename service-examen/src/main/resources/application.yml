server:
  port: 8082
spring:
  application:
    name: examen-api
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/examen
    username: bmbacke
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true